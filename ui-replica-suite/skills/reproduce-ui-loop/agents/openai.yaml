interface:
  display_name: "Reproduce UI Loop"
  short_description: "Rebuild and converge a UI with gpt-5.6-sol xhigh"
  default_prompt: "Reproduce this visual UI in code, use the supplied sprite atlas, and iterate against screenshot differences."
