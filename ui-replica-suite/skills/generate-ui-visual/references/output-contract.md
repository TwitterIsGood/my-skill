# Output Contract

- `visual-target.png`: canonical visual target.
- `run.json`: source type, model, prompt, absolute image path, pixel dimensions, and image mode.

The visual target is immutable after downstream stages begin. Generate a new run rather than silently replacing it.
